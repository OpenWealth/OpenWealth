type: object
required:
  - relationList
  - personDetails
properties:
  relationList:
    type: array
    description: List of relations the person has to this customer relationship.
    items:
      $ref: 'relation.yaml'
  personDetails:
    $ref: 'personDetails.yaml'
