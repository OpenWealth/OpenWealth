type: object
required:
  - source
  - amount
  - currency
properties:
  source:
    $ref: 'source.yaml'
  amount:
    $ref: 'amount.yaml'
  currency:
    $ref: '../../../generics/V2/components/schemas/currency.yaml'
  originsOfAssetsList:
    type: array
    items:
      $ref: 'country.yaml'
