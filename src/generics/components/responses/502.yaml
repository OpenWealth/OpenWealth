headers:
  Content-Type:
    $ref: '../headers/Problem-Content-Type.yaml'
  Content-Language:
    $ref: '../headers/Content-Language.yaml'
  Correlation-Id:
    $ref: '../headers/Correlation-Id.yaml'
description: Bad Gateway - The server received an invalid response from the upstream server.
content:
  application/problem+json:
    schema:
      $ref: 'CommonErrorResponse.yaml'
