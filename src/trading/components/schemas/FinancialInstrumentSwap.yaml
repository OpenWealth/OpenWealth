title: Financial instrument swap agreement
description: Schema of a financial instrument swap agreement
allOf:
  - $ref: 'FinancialInstrumentBase.yaml'
  - type: object
    required:
      - nearSettlementDate
      - farSettlementDate
    properties:
      nearSettlementDate:
        $ref: ../../../generics/components/schemas/Date.yaml
      farSettlementDate:
        $ref: ../../../generics/components/schemas/Date.yaml
