title: Single leg quote
description: Schema of a single leg quote
allOf:
  - $ref: QuoteBase.yaml
  - type: object
    required:
      - legInfo
    properties:
      legInfo:
        $ref: 'QuoteLeg.yaml'
