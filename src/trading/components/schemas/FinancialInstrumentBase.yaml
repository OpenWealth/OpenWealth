type: object
required:
  - type
  - identification
properties:
  type:
    description: Type of ordered instrument
    type: string
    enum:
      - basic
      - forward
      - exchangeTradedDerivative
      - preciousMetal
      - preciousMetalForward
  identification:
    $ref: FinancialInstrumentIdentification.yaml
