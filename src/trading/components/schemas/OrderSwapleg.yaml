title: Swap Leg order
type: object
description: Schema of a swap leg order entity.
allOf:
  - $ref: 'OrderBase.yaml'
  - type: object
    required:
      - nearLeg
      - farLeg
    properties:
      nearLeg:
        $ref: 'OrderLeg.yaml'
      farLeg:
        $ref: 'OrderLeg.yaml'
