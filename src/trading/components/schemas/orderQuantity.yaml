type: object
description: Ordered quantity.
properties:
  amount:
    $ref: 'amount.yaml'
  type:
    type: string
    description: Type of the amount.
    example: unitsNumber
    enum:
      - unitsNumber
      - nominal
