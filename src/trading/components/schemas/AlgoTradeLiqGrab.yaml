title: Liquidity Grabber Algo Trade
description: Schema of a liquidity grabber Algo Trade
allOf:
  - $ref: AlgoTradeBase.yaml
  - type: object
    required:
      - targetRate
    properties:
      urgency:
        description: |
          Indicates the degree of urgency for execution as defined by the client.
        type: string
        enum:
          - ultraPassive
          - passive
          - neutral
          - aggressive
          - superAggressive
