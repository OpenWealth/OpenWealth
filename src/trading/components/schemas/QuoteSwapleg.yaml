title: Swap leg quote
description: Swap leg quote
allOf:
  - $ref: QuoteBase.yaml
  - type: object
    required:
      - nearLeg
      - farLeg
    properties:
      nearLeg:
        $ref: 'QuoteLeg.yaml'
      farLeg:
        $ref: 'QuoteLeg.yaml'
