type: string
description: indicates whether the order quantity is expressed in base instrument or quote instrument. BaseInstrument by default. For swap, the unitReference always reference to nearLeg
enum:
  - baseInstrument
  - quoteInstrument