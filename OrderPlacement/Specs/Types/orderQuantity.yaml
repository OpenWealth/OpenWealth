type: object
description: Ordered quantity.
required:
  - amount
  - type
properties:
  amount:
    $ref: 'amount.yaml'
  type:
    type: string
    description: Type of the amount.
    example: unit
    enum:
      - unit
      - nominal