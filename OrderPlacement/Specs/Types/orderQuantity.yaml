type: object
description: Type for order quantity.
required:
  - amount
  - type
properties:
  amount:
    $ref: 'amount.yaml'
  type:
    type: string
    description: Type of the amount.
    example: unitsNumber
    enum:
      - unitsNumber
      - nominal